export class Server {
    //dev
    public host = "http://localhost:8044";
    public cliente = "http://localhost:4200";
    public userPicture = `${this.host}/image/user`;
    public postPicture = `${this.host}/image/post`;
}
